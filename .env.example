# Copy to `.env` for local development. Public RevenueCat SDK keys (safe in client bundles).
# Get keys from RevenueCat: Project Settings → API keys → App-specific / Public API key.

EXPO_PUBLIC_REVENUECAT_IOS_KEY=
EXPO_PUBLIC_REVENUECAT_ANDROID_KEY=
